server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Proxy /share requests (recipe share pages) to the backend
    location /share/ {
        proxy_pass http://backend:8001/share/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Proxy /api requests to the backend container
    location /api/ {
        proxy_pass http://backend:8001/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        # AI assist calls (ai-edit) can take up to 90s against the model provider;
        # keep this above AIService's httpx timeout so nginx doesn't 504 first.
        proxy_read_timeout 120s;
        proxy_send_timeout 120s;
    }

    # Proxy /uploads requests to the backend container
    location /uploads/ {
        proxy_pass http://backend:8001/uploads/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Serve static files; fall back to index.html for SPA client-side routing
    location / {
        try_files $uri $uri/ /index.html;
    }
}
